apiVersion: v1
kind: Service
metadata:
  labels:
    app: ratingservice
  name: ratingservice
spec:
  ports:
  - name: "8080"
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: ratingservice
  type: ClusterIP
status:
  loadBalancer: {}
